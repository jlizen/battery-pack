[package]
name = "http-battery-pack"
version = "0.1.0"
edition = "2024"
description = "HTTP utilities"
license = "MIT OR Apache-2.0"
readme = "README.md"
keywords = ["battery-pack"]

[dependencies]
battery-pack = "[..]"
# Add your curated dependencies here:
# clap = { version = "4", features = ["derive"] }
# serde = { version = "1", features = ["derive"] }

[dev-dependencies]
battery-pack = { version = "[..]", features = ["cli"] }
snapbox = "1"

[build-dependencies]
battery-pack = { version = "[..]", features = ["build"] }

# Optional: narrow the default set and define named features
# [features]
# default = ["clap", "serde"]
# json = ["serde_json"]

[package.metadata.battery-pack]
hidden = ["battery-pack", "snapbox"]

[package.metadata.battery.templates]
default = { path = "templates/default", description = "Starter template" }
