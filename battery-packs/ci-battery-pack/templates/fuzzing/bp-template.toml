[placeholders.ci_platform]
type = "select"
prompt = "CI platform"
options = ["github", "none"]
default = "github"

[placeholders.repo_owner]
type = "string"
prompt = "Repository owner (org or user)"
default = "OWNER"

[[hints]]
message = "Edit fuzz/fuzz_targets/fuzz_example.rs with your fuzz targets"

[[hints]]
message = "Install cargo-fuzz: cargo install cargo-fuzz"
